{
  "name": "football-stream-recorder",
  "version": "1.0.0",
  "description": "Automated football stream recorder with web dashboard",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd backend && npm run dev",
    "client:dev": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["football", "streaming", "recording", "ffmpeg", "react"],
  "author": "Football Stream Recorder",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}